apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "order-service.fullname" . }}
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      {{- include "order-service.labels" . | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
