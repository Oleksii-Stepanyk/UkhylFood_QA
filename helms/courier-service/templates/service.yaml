apiVersion: v1
kind: Service
metadata:
  name: {{ include "courier-service.fullname" . }}
  labels:
    {{- include "courier-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "courier-service.selectorLabels" . | nindent 4 }}
