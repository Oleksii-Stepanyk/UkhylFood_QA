apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "customer-gateway.fullname" . }}
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      {{- include "customer-gateway.labels" . | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
