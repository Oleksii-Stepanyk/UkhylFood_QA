apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "courier-gateway.fullname" . }}
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      {{- include "courier-gateway.labels" . | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
